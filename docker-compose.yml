version: "3.7"

api:
    env_file: "./API/.env"
    container_name: api
    restart: always
    build:
      context: ./API
      dockerfile: ./Dockerfile
    image: "sianadd/chatbot:firsttry"
    

    ports:
      - "3000:3000"